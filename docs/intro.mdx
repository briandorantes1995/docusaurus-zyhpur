---
sidebar_label: 'Guia de Inicio Rapida'
sidebar_position: 1
---

import MonacoSnippetViewer from '@site/src/components/Monaco/MonacoSnippetViewer';

# Inicio Rapido

# La forma mas sencilla y rapida de probar la api

# Headers requeridos en todas las peticiones:

:::info
**Authorization: Bearer TU_API_KEY**  
**Content-Type: application/json**
:::

:::tip
La Apikey es generada en la seccion de apikeys del dashboard , una vez registrado puedes comenzar a usarla
Si aun no te has registrado, registrate para recibir 50 creditos gratis 
:::

En las siguientes secciones hablaremos mas a fondo sobre cada parametro usable en el renderizado de documentos

<MonacoSnippetViewer
  fileName="api.js"
  language="javascript"
  highlightLines={[2]}
  code={`const response = await fetch('https://api.zyphur.me/render/pdf', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer TU_API_KEY',
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    url: 'https://example.com/reporte',
  })
});

const data = await response.json();`}
/>